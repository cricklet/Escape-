using UnityEngine;
using System.Collections;

public class BillboardSprite : MonoBehaviour {
	void Update () {

		// first rotate on y to get sprite to face camera
		//transform.LookAt (Camera.main.transform.position, Vector3.up);
	}
}
